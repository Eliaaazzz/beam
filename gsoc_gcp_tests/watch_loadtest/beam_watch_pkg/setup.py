import setuptools

setuptools.setup(
    name='beam_watch',
    version='0.1.0',
    description='Standalone Watch transform under validation on Dataflow.',
    packages=['beam_watch'],
    install_requires=[],
)
